service: test-app
image: test-app

ssh:
  user: kamal
  port: 2200

servers:
  web:
    - 127.0.0.1

registry:
  server: localhost:5555

builder:
  arch: arm64

x-provision:
  keys:
    - test/docker/test_key.pub
